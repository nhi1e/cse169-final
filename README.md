Open 3 separate terminal windows or tabs and run the following commands:

```bash
cd bigscreen/
python3 -m http.server 8000

cd smallscreen/
python3 -m http.server 8001

cd server/
node server.js
```
